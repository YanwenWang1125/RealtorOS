# =============================================================================
# RealtorOS Environment Variables
# =============================================================================
# Copy this file to .env in the PROJECT ROOT (same directory as docker-compose.yml)
# and fill in your actual values
# 
# IMPORTANT: 
# - For Docker: Use .env in the PROJECT ROOT (this file location)
# - For local backend dev: You can also have backend/.env
# - DO NOT commit .env to version control (it's in .gitignore)
# - Cursor/VS Code hides .env files by default (security feature)
#   To view: Ctrl+Shift+P -> "Files: Toggle Excluded Files"
# =============================================================================

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL Database Connection
# Format: postgresql+asyncpg://username:password@host:port/database_name
# For Docker: use 'postgres' as hostname
# For local: use 'localhost' as hostname
DATABASE_URL=postgresql+asyncpg://postgres:dev_password_change_in_production@postgres:5432/realtoros

# PostgreSQL Settings (used by docker-compose.yml)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=dev_password_change_in_production
POSTGRES_DB=realtoros

# =============================================================================
# REDIS & CELERY CONFIGURATION
# =============================================================================
# Redis Connection URL
# For Docker: use 'redis' as hostname
# For local: use 'localhost' as hostname
REDIS_URL=redis://redis:6379/0

# Celery Configuration
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1

# =============================================================================
# FASTAPI APPLICATION CONFIGURATION
# =============================================================================
ENVIRONMENT=development
DEBUG=true
API_TITLE=RealtorOS API
API_VERSION=1.0.0

# =============================================================================
# SECURITY & AUTHENTICATION
# =============================================================================
# JWT Secret Key - MUST be at least 32 characters in production
# Generate a secure random key: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-in-production-minimum-32-characters-long

# JWT Algorithm
ALGORITHM=HS256

# Access Token Expiration (in minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# =============================================================================
# GOOGLE OAUTH CONFIGURATION
# =============================================================================
# Google OAuth Client ID and Secret
# Get these from: https://console.cloud.google.com/
# Required for Google Sign-In functionality
# IMPORTANT: GOOGLE_CLIENT_ID must match in both frontend and backend
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# =============================================================================
# OPENAI CONFIGURATION
# =============================================================================
# OpenAI API Key - Required for AI email generation
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# OpenAI Model to use for email generation
OPENAI_MODEL=gpt-4

# Maximum tokens for OpenAI requests
OPENAI_MAX_TOKENS=500

# =============================================================================
# SENDGRID EMAIL CONFIGURATION
# =============================================================================
# SendGrid API Key - Required for sending emails
# Get your API key from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=

# Default sender email address (must be verified in SendGrid)
SENDGRID_FROM_EMAIL=

# Default sender name
SENDGRID_FROM_NAME=RealtorOS

# SendGrid Webhook Verification Key (Optional)
# ECDSA public key for webhook signature verification
# Get from: https://app.sendgrid.com/settings/webhooks
# Format: -----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----
# Leave empty in development
SENDGRID_WEBHOOK_VERIFICATION_KEY=

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Allowed CORS origins (comma-separated)
# Add your frontend URLs here
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================
# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# =============================================================================
# FRONTEND CONFIGURATION (Next.js)
# =============================================================================
# API service URLs (used by frontend)
# These are read by docker-compose.yml and passed to the frontend container
# For Docker: Use localhost since services are exposed on host ports
# For production: Update these to your production API URLs
NEXT_PUBLIC_API_URL=http://localhost:8001
NEXT_PUBLIC_CRM_URL=http://localhost:8002
NEXT_PUBLIC_TASK_URL=http://localhost:8003
NEXT_PUBLIC_EMAIL_URL=http://localhost:8004
NEXT_PUBLIC_ANALYTICS_URL=http://localhost:8006

# Frontend Google Client ID (must match backend GOOGLE_CLIENT_ID)
# This is automatically set from GOOGLE_CLIENT_ID in docker-compose.yml
# If you need a different value, you can set it here
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# =============================================================================
# NOTES
# =============================================================================
# 1. For Docker development:
#    - Use service names as hostnames (postgres, redis)
#    - Example: DATABASE_URL=postgresql+asyncpg://postgres:password@postgres:5432/realtoros
#
# 2. For local development (without Docker):
#    - Use localhost as hostname
#    - Example: DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/realtoros
#
# 3. Required for basic functionality:
#    - DATABASE_URL
#    - SECRET_KEY
#    - All FastAPI settings
#
# 4. Required for email features:
#    - OPENAI_API_KEY
#    - SENDGRID_API_KEY
#    - SENDGRID_FROM_EMAIL
#    - SENDGRID_FROM_NAME
#
# 5. Required for Google Sign-In:
#    - GOOGLE_CLIENT_ID
#    - GOOGLE_CLIENT_SECRET
#
# 6. Production requirements:
#    - SECRET_KEY must be at least 32 characters
#    - DEBUG must be false
#    - ENVIRONMENT must be "production"
#    - Use strong passwords and secure keys

